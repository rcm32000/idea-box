<h1>All Categories</h1>
<% @categories.each do |c| %>
  <p><%= link_to c.name, admin_category_path(c) %></p></br>
<% end %>

<%= button_to 'Create Category', new_admin_category_path, method: 'get' %>
